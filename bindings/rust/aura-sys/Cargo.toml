[package]
name = "aura-sys"
version = "0.7.0"
edition = "2021"
authors = ["Par-B <par.botes@gmail.com>"]
description = "Raw FFI bindings for AuraIO async I/O library"
license = "Apache-2.0"
keywords = ["io_uring", "async", "ffi"]
repository = "https://github.com/Par-B/AuraIO"
readme = "README.md"
categories = ["external-ffi-bindings", "asynchronous"]

[build-dependencies]
bindgen = "0.69"
pkg-config = "0.3"

[features]
default = []
